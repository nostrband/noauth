import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nostr.band',
  appName: '@noauth/ios',
  webDir: './packages/client/build'
};

export default config;
